version: '3'

services:
  postgres:
    image: postgres
    container_name: postgres
    ports:
     - 5432:5432
    environment:
     POSTGRES_PASSWORD: root
    volumes:
     - ./banco_dados/data/postgres:/var/lib/postgresql